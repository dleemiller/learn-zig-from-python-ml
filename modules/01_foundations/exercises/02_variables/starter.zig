const std = @import("std");

pub fn main() void {
    // Declare a constant MAX_SCORE with value 100
    // Declare a mutable variable current_score starting at 0
    // Increment current_score by 25, three times
    // Print "Score: X / Y" where X is current_score and Y is MAX_SCORE
}
